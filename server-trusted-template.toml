# TRUSTED: enable platform exec activities. App policy still controls each deployment.
allowed_exec_activities = "*"
# For deployments with exec activities, --fix adds reviewed configuration digests.
