{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DeploymentToml",
  "type": "object",
  "properties": {
    "activity_wasm": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ActivityWasmComponentConfigToml"
      },
      "default": []
    },
    "activity_stub": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ActivityStubComponentConfigToml"
      },
      "default": []
    },
    "activity_external": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ActivityExternalComponentConfigToml"
      },
      "default": []
    },
    "activity_js": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ActivityJsComponentConfigToml"
      },
      "default": []
    },
    "activity_exec": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/ActivityExecComponentConfigToml"
      },
      "default": []
    },
    "workflow_wasm": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/WorkflowWasmComponentConfigToml"
      },
      "default": []
    },
    "workflow_js": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/WorkflowJsComponentConfigToml"
      },
      "default": []
    },
    "webhook_endpoint_wasm": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/WebhookWasmComponentConfigToml"
      },
      "default": []
    },
    "webhook_endpoint_js": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/WebhookJsComponentConfigToml"
      },
      "default": []
    },
    "cron": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/CronComponentConfigToml"
      },
      "default": []
    }
  },
  "additionalProperties": false,
  "$defs": {
    "ActivityWasmComponentConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/ConfigName"
        },
        "location": {
          "$ref": "#/$defs/ComponentLocationToml"
        },
        "content_digest": {
          "description": "Optional content digest of the WASM file.",
          "type": [
            "string",
            "null"
          ]
        },
        "component_digest": {
          "description": "Override the auto-computed component digest used for locking.\nIf set, this value is used instead of the content digest of the WASM file.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "exec": {
          "$ref": "#/$defs/ExecConfigToml",
          "default": {
            "batch_size": 5,
            "lock_expiry": {
              "seconds": 1
            },
            "tick_sleep": {
              "milliseconds": 200
            },
            "locking_strategy": null,
            "instance_limiter": "unlimited"
          }
        },
        "max_retries": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 5
        },
        "retry_exp_backoff": {
          "$ref": "#/$defs/DurationConfig",
          "default": {
            "milliseconds": 100
          }
        },
        "forward_stdout": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "forward_stderr": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "env_vars": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvVarConfig"
          },
          "default": []
        },
        "logs_store_min_level": {
          "$ref": "#/$defs/LogLevelToml",
          "default": "debug"
        },
        "allowed_host": {
          "description": "Allowed outgoing HTTP hosts with optional method restrictions and secrets.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AllowedHostToml"
          },
          "default": []
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "location"
      ]
    },
    "ConfigName": {
      "description": "Activity, Webhook, Workflow or a Http server",
      "type": "string"
    },
    "ComponentLocationToml": {
      "description": "Location of a WASM component.\nThe OCI reference is kept as a string (without the `oci://` prefix); it is\nvalidated and normalized by the obelisk server before canonicalization.",
      "type": "string"
    },
    "ExecConfigToml": {
      "type": "object",
      "properties": {
        "batch_size": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 5
        },
        "lock_expiry": {
          "$ref": "#/$defs/DurationConfig",
          "default": {
            "seconds": 1
          }
        },
        "tick_sleep": {
          "$ref": "#/$defs/DurationConfig",
          "default": {
            "milliseconds": 200
          }
        },
        "locking_strategy": {
          "anyOf": [
            {
              "$ref": "#/$defs/LockingStrategy"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "instance_limiter": {
          "$ref": "#/$defs/InflightSemaphore",
          "default": "unlimited"
        }
      },
      "additionalProperties": false
    },
    "DurationConfig": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "milliseconds": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "milliseconds"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "seconds": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "seconds"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "minutes": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "minutes"
          ],
          "additionalProperties": false
        },
        {
          "type": "object",
          "properties": {
            "hours": {
              "type": "integer",
              "format": "uint64",
              "minimum": 0
            }
          },
          "required": [
            "hours"
          ],
          "additionalProperties": false
        }
      ]
    },
    "LockingStrategy": {
      "oneOf": [
        {
          "description": "Select all exported FFQNs",
          "type": "string",
          "const": "by_ffqns"
        },
        {
          "description": "Select by component digest",
          "type": "string",
          "const": "by_component_digest"
        },
        {
          "description": "Only applicable for workflows: Same as `ByFffqns`, with automatic upgrade",
          "type": "string",
          "const": "auto"
        }
      ]
    },
    "InflightSemaphore": {
      "anyOf": [
        {
          "$ref": "#/$defs/Unlimited"
        },
        {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      ]
    },
    "Unlimited": {
      "type": "string",
      "enum": [
        "unlimited"
      ]
    },
    "ComponentStdOutputToml": {
      "type": "string",
      "enum": [
        "none",
        "stdout",
        "stderr",
        "db"
      ]
    },
    "EnvVarConfig": {
      "anyOf": [
        {
          "description": "Forward from host: `\"KEY\"`",
          "type": "string"
        },
        {
          "description": "Set to value: `{key = \"KEY\", value = \"foo\"}` (supports `${VAR}` interpolation)",
          "type": "object",
          "properties": {
            "key": {
              "type": "string"
            },
            "value": {
              "type": "string"
            }
          },
          "required": [
            "key",
            "value"
          ]
        }
      ]
    },
    "LogLevelToml": {
      "type": "string",
      "enum": [
        "off",
        "trace",
        "debug",
        "info",
        "warn",
        "error"
      ]
    },
    "AllowedHostToml": {
      "description": "An allowed outgoing HTTP host with optional method restrictions and secrets.",
      "type": "object",
      "properties": {
        "pattern": {
          "description": "Host pattern (e.g. `\"api.example.com\"`, `\"*.example.com\"`, `\"http://localhost:8080\"`).",
          "type": "string"
        },
        "methods": {
          "description": "Allowed HTTP methods.\n- Omit to allow nothing (warning emitted).\n- `methods = \"*\"` or `methods = [\"*\"]` to allow all methods.\n- `methods = [\"GET\", \"POST\"]` to allow specific methods.\n- `methods = []` to allow nothing (warning emitted).",
          "anyOf": [
            {
              "$ref": "#/$defs/MethodsInput"
            },
            {
              "type": "null"
            }
          ]
        },
        "secrets": {
          "description": "Optional secrets for this host.",
          "anyOf": [
            {
              "$ref": "#/$defs/AllowedHostSecretsToml"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "pattern"
      ]
    },
    "MethodsInput": {
      "description": "Input for method restrictions in TOML configuration.\nSupports both `methods = \"*\"` and `methods = [\"GET\", \"POST\"]` syntax.",
      "anyOf": [
        {
          "description": "All methods allowed (from `methods = \"*\"`).",
          "$ref": "#/$defs/MethodsInputStar"
        },
        {
          "description": "Specific methods list (from `methods = [\"GET\", \"POST\"]`).",
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ]
    },
    "MethodsInputStar": {
      "type": "null"
    },
    "AllowedHostSecretsToml": {
      "description": "Secrets configuration for an allowed host.",
      "type": "object",
      "properties": {
        "env_vars": {
          "description": "Env vars using the same syntax as top-level `env_vars`.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvVarConfig"
          }
        },
        "replace_in": {
          "description": "Where in the request to perform replacement.\nDefault: empty (no replacement — deny by default).",
          "type": "array",
          "items": {
            "$ref": "#/$defs/ReplaceIn"
          },
          "default": []
        }
      },
      "additionalProperties": false,
      "required": [
        "env_vars"
      ]
    },
    "ReplaceIn": {
      "description": "Where in the outgoing request placeholders are replaced.",
      "type": "string",
      "enum": [
        "headers",
        "body",
        "params"
      ]
    },
    "ActivityStubComponentConfigToml": {
      "anyOf": [
        {
          "$ref": "#/$defs/ActivityStubFileConfigToml"
        },
        {
          "$ref": "#/$defs/ActivityStubExtInlineConfigToml"
        }
      ]
    },
    "ActivityStubFileConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/ConfigName"
        },
        "location": {
          "$ref": "#/$defs/ComponentLocationToml"
        },
        "content_digest": {
          "description": "Optional content digest of the WASM file.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "location"
      ]
    },
    "ActivityStubExtInlineConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "description": "Component name. Optional — defaults to `{ifc_name}.{function_name}` from `ffqn`.",
          "anyOf": [
            {
              "$ref": "#/$defs/ConfigName"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "ffqn": {
          "type": "string"
        },
        "params": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/$defs/JsParamToml"
          },
          "default": null
        },
        "return_type": {
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "ffqn"
      ]
    },
    "JsParamToml": {
      "description": "A parameter declaration for a JS activity function.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Parameter name (used in WIT metadata).\nStored in kebab-case for WIT compatibility.",
          "type": "string"
        },
        "type": {
          "description": "WIT type string, e.g. `string`, `u32`, `list<string>`, `option<u64>`.",
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "type"
      ]
    },
    "ActivityExternalComponentConfigToml": {
      "anyOf": [
        {
          "$ref": "#/$defs/ActivityExternalFileConfigToml"
        },
        {
          "$ref": "#/$defs/ActivityStubExtInlineConfigToml"
        }
      ]
    },
    "ActivityExternalFileConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/ConfigName"
        },
        "location": {
          "$ref": "#/$defs/ComponentLocationToml"
        },
        "content_digest": {
          "description": "Optional content digest of the WASM file.",
          "type": [
            "string",
            "null"
          ]
        },
        "component_digest": {
          "description": "Override the auto-computed component digest used for locking.\nIf set, this value is used instead of the content digest of the WASM file.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "location"
      ]
    },
    "ActivityJsComponentConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "description": "Component name. Optional when `ffqn` is specified — defaults to `{ifc_name}.{function_name}`.",
          "anyOf": [
            {
              "$ref": "#/$defs/ConfigName"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "location": {
          "description": "Location of the JavaScript source file.\nSupports local file paths and OCI registry references (`oci://...`).",
          "anyOf": [
            {
              "$ref": "#/$defs/JsLocationToml"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "content": {
          "description": "Inline JavaScript source embedded in the TOML.\nExactly one of `location` or `content` must be set.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "content_digest": {
          "description": "Content digest of the JS source file.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "component_digest": {
          "description": "Override the auto-computed component digest used for locking.\nIf set, this value is used instead of the digest derived from the JS source, ffqn, and params.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "ffqn": {
          "type": "string"
        },
        "params": {
          "description": "Custom parameters for the JS function.\nEach entry has a `name` and a WIT `type` (e.g. `string`, `u32`, `list<string>`).\nDefaults to no parameters.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/JsParamToml"
          },
          "default": []
        },
        "exec": {
          "$ref": "#/$defs/ExecConfigToml",
          "default": {
            "batch_size": 5,
            "lock_expiry": {
              "seconds": 1
            },
            "tick_sleep": {
              "milliseconds": 200
            },
            "locking_strategy": null,
            "instance_limiter": "unlimited"
          }
        },
        "max_retries": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 5
        },
        "retry_exp_backoff": {
          "$ref": "#/$defs/DurationConfig",
          "default": {
            "milliseconds": 100
          }
        },
        "forward_stdout": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "forward_stderr": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "logs_store_min_level": {
          "$ref": "#/$defs/LogLevelToml",
          "default": "debug"
        },
        "env_vars": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvVarConfig"
          },
          "default": []
        },
        "allowed_host": {
          "description": "Allowed outgoing HTTP hosts with optional method restrictions and secrets.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AllowedHostToml"
          },
          "default": []
        },
        "return_type": {
          "description": "WIT return type. Defaults to `result`.\nMust be `result<T, string>` — the error type must be `string` since JS throws strings.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "ffqn"
      ]
    },
    "JsLocationToml": {
      "description": "Location of a JavaScript source file.\nSupports local file paths and OCI registry references (`oci://...`).\nOn-disk format only; replaced by [`ScriptLocationResolved`] before transmission and hash computation.",
      "type": "string"
    },
    "ActivityExecComponentConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "description": "Component name. Optional when `ffqn` is specified — defaults to `{ifc_name}.{function_name}`.",
          "anyOf": [
            {
              "$ref": "#/$defs/ConfigName"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "location": {
          "description": "Location of the exec script.\nSupports local file paths and OCI registry references (`oci://...`).",
          "anyOf": [
            {
              "$ref": "#/$defs/JsLocationToml"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "content": {
          "description": "Inline script content embedded in the TOML.\nExactly one of `location` or `content` must be set.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "content_digest": {
          "description": "Content digest of the exec script.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "ffqn": {
          "type": "string"
        },
        "params": {
          "description": "Custom parameters for the exec activity.\nEach entry has a `name` and a WIT `type` (e.g. `string`, `u32`, `list<string>`).",
          "type": "array",
          "items": {
            "$ref": "#/$defs/JsParamToml"
          },
          "default": []
        },
        "return_type": {
          "description": "WIT return type. Defaults to `result`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "component_digest": {
          "description": "Override the auto-computed component digest used for locking.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "exec": {
          "$ref": "#/$defs/ExecConfigToml",
          "default": {
            "batch_size": 5,
            "lock_expiry": {
              "seconds": 1
            },
            "tick_sleep": {
              "milliseconds": 200
            },
            "locking_strategy": null,
            "instance_limiter": "unlimited"
          }
        },
        "max_retries": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "default": 5
        },
        "retry_exp_backoff": {
          "$ref": "#/$defs/DurationConfig",
          "default": {
            "milliseconds": 100
          }
        },
        "forward_stdout": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "forward_stderr": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "logs_store_min_level": {
          "$ref": "#/$defs/LogLevelToml",
          "default": "debug"
        },
        "env_vars": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvVarConfig"
          },
          "default": []
        },
        "max_output_bytes": {
          "description": "Maximum bytes collected from stdout to form the response.\nExceeding the limit fails the execution.\nNot used when `return_type` is result (default), since the response carries no data.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 4096
        },
        "secrets": {
          "description": "Secrets pushed to stdin. See `ExecSecretsToml`.",
          "anyOf": [
            {
              "$ref": "#/$defs/ExecSecretsToml"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "params_via_stdin": {
          "description": "Pass parameters to the program via the stdin JSON `parameters` array instead\nof argv. Use this for large payloads that would exceed the `execve` argument-size\nlimit. Defaults to `false` (parameters passed as command-line arguments).",
          "type": "boolean",
          "default": false
        }
      },
      "additionalProperties": false,
      "required": [
        "ffqn"
      ]
    },
    "ExecSecretsToml": {
      "description": "Secrets configuration for exec activities.",
      "type": "object",
      "properties": {
        "env_vars": {
          "description": "Secret entries resolved from environment variables.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/SecretEnvVarToml"
          },
          "default": []
        }
      },
      "additionalProperties": false
    },
    "SecretEnvVarToml": {
      "description": "Secret entry: resolved from environment variables at startup.",
      "type": "object",
      "properties": {
        "name": {
          "description": "Name used to reference this secret in the `secrets.stdin` function.",
          "type": "string"
        },
        "value": {
          "description": "Value supporting `${VAR}` interpolation from host environment.",
          "type": "string",
          "default": ""
        }
      },
      "additionalProperties": false,
      "required": [
        "name"
      ]
    },
    "WorkflowWasmComponentConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/ConfigName"
        },
        "location": {
          "$ref": "#/$defs/ComponentLocationToml"
        },
        "content_digest": {
          "description": "Optional content digest of the WASM file.",
          "type": [
            "string",
            "null"
          ]
        },
        "component_digest": {
          "description": "Override the auto-computed component digest used for locking.\nIf set, this value is used instead of the content digest of the WASM file.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "exec": {
          "$ref": "#/$defs/ExecConfigToml",
          "default": {
            "batch_size": 5,
            "lock_expiry": {
              "seconds": 1
            },
            "tick_sleep": {
              "milliseconds": 200
            },
            "locking_strategy": null,
            "instance_limiter": "unlimited"
          }
        },
        "retry_exp_backoff": {
          "$ref": "#/$defs/DurationConfig",
          "default": {
            "milliseconds": 100
          }
        },
        "blocking_strategy": {
          "$ref": "#/$defs/BlockingStrategyConfigToml",
          "default": "await"
        },
        "backtrace": {
          "$ref": "#/$defs/ComponentBacktraceConfig",
          "default": {
            "sources": {}
          }
        },
        "stub_wasi": {
          "type": "boolean",
          "default": false
        },
        "lock_extension": {
          "type": "boolean",
          "default": true
        },
        "logs_store_min_level": {
          "$ref": "#/$defs/LogLevelToml",
          "default": "debug"
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "location"
      ]
    },
    "BlockingStrategyConfigToml": {
      "anyOf": [
        {
          "$ref": "#/$defs/BlockingStrategyConfigCustomized"
        },
        {
          "$ref": "#/$defs/BlockingStrategyConfigSimple"
        }
      ]
    },
    "BlockingStrategyConfigCustomized": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "non_blocking_event_batching": {
              "type": "integer",
              "format": "uint32",
              "minimum": 0,
              "default": 100
            },
            "kind": {
              "type": "string",
              "const": "await"
            }
          },
          "additionalProperties": false,
          "required": [
            "kind"
          ]
        }
      ]
    },
    "BlockingStrategyConfigSimple": {
      "type": "string",
      "enum": [
        "interrupt",
        "await"
      ]
    },
    "ComponentBacktraceConfig": {
      "type": "object",
      "properties": {
        "sources": {
          "description": "Maps a frame-symbol key to a backtrace source file path. On-disk format only;\nresolved to `ComponentBacktraceConfigResolved` before transmission and hash\ncomputation. A relative path is deployment-dir-relative (a leading\n`${DEPLOYMENT_DIR}/` is accepted for backcompat); absolute paths are rejected.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/$defs/BacktraceSourceToml"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "sources"
      ]
    },
    "BacktraceSourceToml": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "object",
          "properties": {
            "path": {
              "type": "string"
            },
            "content_digest": {
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "path"
          ]
        }
      ]
    },
    "WorkflowJsComponentConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "description": "Component name. Optional when `ffqn` is specified — defaults to `{ifc_name}.{function_name}`.",
          "anyOf": [
            {
              "$ref": "#/$defs/ConfigName"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "location": {
          "description": "Location of the JavaScript source file.\nSupports local file paths and OCI registry references (`oci://...`).",
          "anyOf": [
            {
              "$ref": "#/$defs/JsLocationToml"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "content": {
          "description": "Inline JavaScript source embedded in the TOML.\nExactly one of `location` or `content` must be set.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "content_digest": {
          "description": "Content digest of the JS source file.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "component_digest": {
          "description": "Override the auto-computed component digest used for locking.\nIf set, this value is used instead of the digest derived from the JS source, ffqn, and params.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "ffqn": {
          "type": "string"
        },
        "params": {
          "description": "Custom parameters for the JS workflow function.\nEach entry has a `name` and a WIT `type` (e.g. `string`, `u32`, `list<string>`).\nDefaults to no parameters.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/JsParamToml"
          },
          "default": []
        },
        "exec": {
          "$ref": "#/$defs/ExecConfigToml",
          "default": {
            "batch_size": 5,
            "lock_expiry": {
              "seconds": 1
            },
            "tick_sleep": {
              "milliseconds": 200
            },
            "locking_strategy": null,
            "instance_limiter": "unlimited"
          }
        },
        "retry_exp_backoff": {
          "$ref": "#/$defs/DurationConfig",
          "default": {
            "milliseconds": 100
          }
        },
        "blocking_strategy": {
          "$ref": "#/$defs/BlockingStrategyConfigToml",
          "default": "await"
        },
        "lock_extension": {
          "type": "boolean",
          "default": true
        },
        "logs_store_min_level": {
          "$ref": "#/$defs/LogLevelToml",
          "default": "debug"
        },
        "return_type": {
          "description": "WIT return type. Defaults to `result`.\nMust be `result`, `result<T>`, `result<T, string>`, or\n`result<T, variant { execution-failed, ... }>`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      },
      "additionalProperties": false,
      "required": [
        "ffqn"
      ]
    },
    "WebhookWasmComponentConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/ConfigName"
        },
        "location": {
          "$ref": "#/$defs/ComponentLocationToml"
        },
        "content_digest": {
          "description": "Optional content digest of the WASM file.",
          "type": [
            "string",
            "null"
          ]
        },
        "http_server": {
          "$ref": "#/$defs/ConfigName",
          "default": "external"
        },
        "routes": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/WebhookRoute"
          }
        },
        "forward_stdout": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "forward_stderr": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "env_vars": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvVarConfig"
          },
          "default": []
        },
        "backtrace": {
          "$ref": "#/$defs/ComponentBacktraceConfig",
          "default": {
            "sources": {}
          }
        },
        "logs_store_min_level": {
          "$ref": "#/$defs/LogLevelToml",
          "default": "debug"
        },
        "allowed_host": {
          "description": "Allowed outgoing HTTP hosts with optional method restrictions and secrets.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AllowedHostToml"
          },
          "default": []
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "location",
        "routes"
      ]
    },
    "WebhookRoute": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "$ref": "#/$defs/WebhookRouteDetail"
        }
      ]
    },
    "WebhookRouteDetail": {
      "type": "object",
      "properties": {
        "methods": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "route": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "required": [
        "route"
      ]
    },
    "WebhookJsComponentConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/ConfigName"
        },
        "location": {
          "description": "Location of the JavaScript source file.\nSupports local file paths and OCI registry references (`oci://...`).",
          "anyOf": [
            {
              "$ref": "#/$defs/JsLocationToml"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "content": {
          "description": "Inline JavaScript source embedded in the TOML.\nExactly one of `location` or `content` must be set.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "content_digest": {
          "description": "Content digest of the JS source file.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "http_server": {
          "description": "The HTTP server to bind this webhook to.",
          "$ref": "#/$defs/ConfigName",
          "default": "external"
        },
        "routes": {
          "description": "Routes that this webhook responds to.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/WebhookRoute"
          }
        },
        "forward_stdout": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "forward_stderr": {
          "$ref": "#/$defs/ComponentStdOutputToml",
          "default": "db"
        },
        "logs_store_min_level": {
          "$ref": "#/$defs/LogLevelToml",
          "default": "debug"
        },
        "env_vars": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/EnvVarConfig"
          },
          "default": []
        },
        "allowed_host": {
          "description": "Allowed outgoing HTTP hosts with optional method restrictions and secrets.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/AllowedHostToml"
          },
          "default": []
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "routes"
      ]
    },
    "CronComponentConfigToml": {
      "type": "object",
      "properties": {
        "name": {
          "$ref": "#/$defs/ConfigName"
        },
        "ffqn": {
          "description": "Fully qualified function name of the target to invoke on each schedule tick.",
          "type": "string"
        },
        "params": {
          "description": "JSON-encoded parameters to pass to the target function.",
          "type": "string",
          "default": "[]"
        },
        "schedule": {
          "description": "Cron expression or `@once`, `@daily`, `@hourly`, `@weekly`, `@monthly`, `@yearly`.",
          "type": "string"
        },
        "exec": {
          "$ref": "#/$defs/ExecConfigToml",
          "default": {
            "batch_size": 5,
            "lock_expiry": {
              "seconds": 1
            },
            "tick_sleep": {
              "milliseconds": 200
            },
            "locking_strategy": null,
            "instance_limiter": "unlimited"
          }
        }
      },
      "additionalProperties": false,
      "required": [
        "name",
        "ffqn",
        "schedule"
      ]
    }
  }
}
