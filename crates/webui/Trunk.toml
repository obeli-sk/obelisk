[build]
release = true   # Build in release mode.
filehash = false # Whether to include hash values in the output file names.

[[proxy]]
backend = "https://127.0.0.1:5005" # Address to proxy requests to
rewrite = "/api"                   # Remove /api path prefix
