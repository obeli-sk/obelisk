package my-org:my-workflow

interface host-activities {
    sleep: func(millis: u64)
}

interface my-activity {
    send: func(/* idempotency-id: string, email: email */) -> result<string, string>
}

world sleepy-workflow {
    import host-activities
    import my-activity
    export execute: func() -> string
    export second: func() -> string
}
