package testing:sleep-workflow;

interface workflow {
    use obelisk:workflow/host-activities.{duration};
    sleep-host-activity: func(millis: u32);
    sleep-activity: func(millis: u32);
    reschedule: func(nanos: duration, iterations: u8);
}
