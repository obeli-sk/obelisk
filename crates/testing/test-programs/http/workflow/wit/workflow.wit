package testing:http-workflow;

interface workflow {
    execute: func(port: u16) -> string;
    get: func(authority: string, path: string) -> result<string, string>;
}

world any {
    import testing:http/http-get;
    export workflow;
}
