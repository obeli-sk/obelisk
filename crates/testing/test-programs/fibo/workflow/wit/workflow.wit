package testing:fibo-workflow;

interface workflow {
    fibow: func(n: u8, iterations: u8) -> string;
    fiboa: func(n: u8, iterations: u8) -> string;
}

world any {
    import testing:fibo/fibo;
    export workflow;
}
